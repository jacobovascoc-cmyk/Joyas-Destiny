{
  "name": "joyeria-destiny",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio de Joyería Destiny. Tailwind se compila a un CSS estático: la página publicada no depende de ningún recurso externo.",
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/input.css -o tailwind.css --minify",
    "watch": "tailwindcss -c tailwind.config.js -i src/input.css -o tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
